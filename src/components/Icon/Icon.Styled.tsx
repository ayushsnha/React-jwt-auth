import styled from 'styled-components';

const IconStyle = styled.button`
    background: none;
    border: none;
    cursor: pointer;
    &:focus {
        outline: none;
    }
`;

export default IconStyle;
